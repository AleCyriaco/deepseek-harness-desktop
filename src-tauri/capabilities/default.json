{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window (an external webview of the harness UI)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
