name: Release

# Builds the desktop bundles for macOS, Windows and Linux and attaches them to
# a draft GitHub Release. Push a `v*` tag to trigger it, then review and
# publish the draft by hand.
#
# Bundles produced here are UNSIGNED unless signing secrets are configured;
# see docs/DEVELOPMENT.md#code-signing.

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.label }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            label: macOS (Apple Silicon)
            args: --target aarch64-apple-darwin
            rust-targets: aarch64-apple-darwin
          - os: macos-latest
            label: macOS (Intel)
            args: --target x86_64-apple-darwin
            rust-targets: x86_64-apple-darwin
          - os: ubuntu-latest
            label: Linux
            args: ''
            rust-targets: ''
          - os: windows-latest
            label: Windows
            args: ''
            rust-targets: ''
    steps:
      - uses: actions/checkout@v5

      - name: Install Linux build dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            build-essential \
            curl \
            wget \
            file \
            libssl-dev \
            libgtk-3-dev

      - uses: actions/setup-node@v5
        with:
          node-version: 20

      - name: Install the Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: ${{ matrix.label }}

      - name: Install the Tauri CLI
        run: npm ci || npm install

      - name: Build and draft the release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: DeepSeek Harness Desktop ${{ github.ref_name }}
          releaseBody: |
            See [CHANGELOG.md](https://github.com/AleCyriaco/dsh-desktop/blob/main/CHANGELOG.md) for what changed.

            These bundles are unsigned: on macOS you may need
            `xattr -dr com.apple.quarantine "DeepSeek Harness Desktop.app"`.
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
